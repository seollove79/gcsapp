import React from 'react';
import styles from '../css/FullscreenLayer.module.css'; // CSS 모듈 가져오기

const FullscreenLayer = () => {
    return (
        <div className={styles.fullscreenLayer}>
        </div>
    );
};

export default FullscreenLayer;
